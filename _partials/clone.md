<!-- zh --> ## 📂 【可选】克隆仓库
<!-- en --> ## 📂 [Optional] Clone the Repository

<!-- zh --> 仓库仅供开发参考，不是启用模型的必要条件
<!-- en --> The repository is for development reference only and is not required to run the model.

```bash
mkdir -p ~/workspace
cd ~/workspace
git clone https://github.com/deepseek-ai/{{MODEL_NAME}}.git
```
